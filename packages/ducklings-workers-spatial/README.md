# Ducklings spatial for Workers

Experimental private package with DuckDB spatial, GEOS, PROJ, Parquet, and httpfs; GDAL and lakehouse extensions are omitted by default.

See [build and usage instructions](../../docs/spatial-workers.md). Use `prepareAsync()` for remote Parquet statements and serialize native execution per Wasm instance.
